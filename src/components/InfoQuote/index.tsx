import React from 'react';
import { InfoCircleFilled } from '@ant-design/icons';

import './index.scss';

export default function InfoQuote({ children }) {
  return <div className="info-quote">{children}</div>;
}
